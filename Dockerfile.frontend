FROM node:20-alpine

WORKDIR /app

RUN corepack enable && corepack prepare pnpm@10.14.0 --activate
COPY storm-clone/package.json storm-clone/pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY storm-clone/ ./

RUN pnpm run build

EXPOSE 5173

CMD ["pnpm", "run", "dev", "--host"]

